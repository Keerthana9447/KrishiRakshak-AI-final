# Disease detection needs NO API key (local model)
# Weather uses Open-Meteo by default (also no key)

# Optional: OpenWeatherMap for more detailed weather data
# OPENWEATHER_API_KEY=your_key_here

VITE_API_URL=http://localhost:8000
